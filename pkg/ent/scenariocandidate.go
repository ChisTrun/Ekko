// Code generated by ent, DO NOT EDIT.

package ent

import (
	"ekko/pkg/ent/scenario"
	"ekko/pkg/ent/scenariocandidate"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// ScenarioCandidate is the model entity for the ScenarioCandidate schema.
type ScenarioCandidate struct {
	config `json:"-"`
	// ID of the ent.
	ID uint64 `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// CandidateID holds the value of the "candidate_id" field.
	CandidateID uint64 `json:"candidate_id,omitempty"`
	// ScenarioID holds the value of the "scenario_id" field.
	ScenarioID uint64 `json:"scenario_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ScenarioCandidateQuery when eager-loading is set.
	Edges        ScenarioCandidateEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ScenarioCandidateEdges holds the relations/edges for other nodes in the graph.
type ScenarioCandidateEdges struct {
	// Scenario holds the value of the scenario edge.
	Scenario *Scenario `json:"scenario,omitempty"`
	// Attempts holds the value of the attempts edge.
	Attempts []*SubmissionAttempt `json:"attempts,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ScenarioOrErr returns the Scenario value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ScenarioCandidateEdges) ScenarioOrErr() (*Scenario, error) {
	if e.Scenario != nil {
		return e.Scenario, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: scenario.Label}
	}
	return nil, &NotLoadedError{edge: "scenario"}
}

// AttemptsOrErr returns the Attempts value or an error if the edge
// was not loaded in eager-loading.
func (e ScenarioCandidateEdges) AttemptsOrErr() ([]*SubmissionAttempt, error) {
	if e.loadedTypes[1] {
		return e.Attempts, nil
	}
	return nil, &NotLoadedError{edge: "attempts"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ScenarioCandidate) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case scenariocandidate.FieldID, scenariocandidate.FieldCandidateID, scenariocandidate.FieldScenarioID:
			values[i] = new(sql.NullInt64)
		case scenariocandidate.FieldCreatedAt, scenariocandidate.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ScenarioCandidate fields.
func (sc *ScenarioCandidate) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case scenariocandidate.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			sc.ID = uint64(value.Int64)
		case scenariocandidate.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				sc.CreatedAt = value.Time
			}
		case scenariocandidate.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				sc.UpdatedAt = value.Time
			}
		case scenariocandidate.FieldCandidateID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field candidate_id", values[i])
			} else if value.Valid {
				sc.CandidateID = uint64(value.Int64)
			}
		case scenariocandidate.FieldScenarioID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field scenario_id", values[i])
			} else if value.Valid {
				sc.ScenarioID = uint64(value.Int64)
			}
		default:
			sc.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ScenarioCandidate.
// This includes values selected through modifiers, order, etc.
func (sc *ScenarioCandidate) Value(name string) (ent.Value, error) {
	return sc.selectValues.Get(name)
}

// QueryScenario queries the "scenario" edge of the ScenarioCandidate entity.
func (sc *ScenarioCandidate) QueryScenario() *ScenarioQuery {
	return NewScenarioCandidateClient(sc.config).QueryScenario(sc)
}

// QueryAttempts queries the "attempts" edge of the ScenarioCandidate entity.
func (sc *ScenarioCandidate) QueryAttempts() *SubmissionAttemptQuery {
	return NewScenarioCandidateClient(sc.config).QueryAttempts(sc)
}

// Update returns a builder for updating this ScenarioCandidate.
// Note that you need to call ScenarioCandidate.Unwrap() before calling this method if this ScenarioCandidate
// was returned from a transaction, and the transaction was committed or rolled back.
func (sc *ScenarioCandidate) Update() *ScenarioCandidateUpdateOne {
	return NewScenarioCandidateClient(sc.config).UpdateOne(sc)
}

// Unwrap unwraps the ScenarioCandidate entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sc *ScenarioCandidate) Unwrap() *ScenarioCandidate {
	_tx, ok := sc.config.driver.(*txDriver)
	if !ok {
		panic("ent: ScenarioCandidate is not a transactional entity")
	}
	sc.config.driver = _tx.drv
	return sc
}

// String implements the fmt.Stringer.
func (sc *ScenarioCandidate) String() string {
	var builder strings.Builder
	builder.WriteString("ScenarioCandidate(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sc.ID))
	builder.WriteString("created_at=")
	builder.WriteString(sc.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(sc.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("candidate_id=")
	builder.WriteString(fmt.Sprintf("%v", sc.CandidateID))
	builder.WriteString(", ")
	builder.WriteString("scenario_id=")
	builder.WriteString(fmt.Sprintf("%v", sc.ScenarioID))
	builder.WriteByte(')')
	return builder.String()
}

// ScenarioCandidates is a parsable slice of ScenarioCandidate.
type ScenarioCandidates []*ScenarioCandidate
